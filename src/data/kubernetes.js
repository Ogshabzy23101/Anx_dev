import {
  expectedYamlValue,
  kubernetesRules as k,
  requiredString,
  requiredYamlKey,
} from "../utils/kubernetesValidation.js";
import { kubernetesExtraPractice } from "./kubernetesExtraPractice.js";

export {
  kubernetesCategories,
  kubernetesCommandCatalog,
  kubernetesCommandQuiz,
  kubernetesFlashcards,
  kubernetesMultipleChoice,
  kubernetesReference,
} from "./kubernetesCatalog.js";

const legacyKubernetesReference = [
  {
    category: "Cluster fundamentals",
    commands: [
      { command: "Cluster", description: "The control plane and worker nodes that run Kubernetes workloads." },
      { command: "Node", description: "A worker machine that runs Pods through the kubelet and container runtime." },
      { command: "Pod", description: "The smallest deployable unit, containing one or more tightly coupled containers." },
      { command: "Namespace", description: "A logical boundary for namespaced resources, access, and quotas." },
      { command: "Labels / selectors", description: "Key-value metadata and queries that connect controllers and Services to resources." },
    ],
  },
  {
    category: "Workloads",
    commands: [
      { command: "ReplicaSet", description: "Maintains a requested number of identical Pod replicas." },
      { command: "Deployment", description: "Declaratively manages ReplicaSets and rolling application updates." },
      { command: "StatefulSet", description: "Runs ordered Pods with stable identities and persistent storage." },
      { command: "DaemonSet", description: "Runs a Pod on every eligible Node, often for agents." },
      { command: "Job / CronJob", description: "Runs finite work once or repeatedly on a schedule." },
      { command: "HPA", description: "Horizontally changes replica count from observed metrics." },
    ],
  },
  {
    category: "Networking",
    commands: [
      { command: "Service", description: "Provides a stable virtual IP and DNS name for selected Pods." },
      { command: "ClusterIP", description: "Exposes a Service only inside the cluster." },
      { command: "NodePort", description: "Exposes a Service on a static port of every Node." },
      { command: "LoadBalancer", description: "Requests an external load balancer from a supported provider." },
      { command: "Ingress", description: "Defines HTTP/HTTPS routing to Services through an Ingress controller." },
    ],
  },
  {
    category: "Configuration & storage",
    commands: [
      { command: "ConfigMap", description: "Stores non-sensitive configuration for Pods." },
      { command: "Secret", description: "Stores sensitive values, encoded at rest in manifest data fields." },
      { command: "Volume", description: "Makes storage available to containers in a Pod." },
      { command: "PersistentVolume", description: "Represents cluster storage with a lifecycle independent of Pods." },
      { command: "PersistentVolumeClaim", description: "A workload request for persistent storage." },
    ],
  },
  {
    category: "Identity & RBAC",
    commands: [
      { command: "ServiceAccount", description: "Provides an identity for processes running in Pods." },
      { command: "Role", description: "Defines namespaced RBAC permissions." },
      { command: "RoleBinding", description: "Grants a Role to users, groups, or ServiceAccounts." },
    ],
  },
  {
    category: "kubectl workflow",
    commands: [
      { command: "kubectl apply -f app.yaml", description: "Create or declaratively update resources from a manifest." },
      { command: "kubectl get pods", description: "List resources and concise status information." },
      { command: "kubectl describe pod nginx", description: "Show detailed state, conditions, and related events." },
      { command: "kubectl logs backend -f", description: "Read and follow container logs." },
      { command: "kubectl exec -it backend -- sh", description: "Run an interactive command inside a container." },
    ],
  },
];

const legacyKubernetesFlashcards = [
  { id: "k8s-fc-pod", front: "What is a Pod?", back: "The smallest Kubernetes deployable unit, wrapping one or more containers that share networking and storage." },
  { id: "k8s-fc-deployment", front: "What is a Deployment?", back: "A controller that manages ReplicaSets and declarative rolling updates for stateless workloads." },
  { id: "k8s-fc-service", front: "What is the purpose of a Service?", back: "It provides stable discovery and networking for a changing set of selected Pods." },
  { id: "k8s-fc-service-types", front: "How do ClusterIP, NodePort, and LoadBalancer differ?", back: "ClusterIP is internal, NodePort opens a port on each Node, and LoadBalancer provisions external access through a provider." },
  { id: "k8s-fc-ingress", front: "What does Ingress do?", back: "It describes HTTP/HTTPS host and path routing to Services through an Ingress controller." },
  { id: "k8s-fc-configmap", front: "What is a ConfigMap?", back: "A resource for non-sensitive configuration data consumed by Pods." },
  { id: "k8s-fc-secret", front: "What is a Secret?", back: "A resource for sensitive values such as credentials, tokens, or certificates." },
  { id: "k8s-fc-namespace", front: "What is a Namespace?", back: "A logical scope for resource names, RBAC, quotas, and team separation." },
  { id: "k8s-fc-serviceaccount", front: "What is a ServiceAccount?", back: "A namespaced identity used by workloads and automation when calling the Kubernetes API." },
  { id: "k8s-fc-role-binding", front: "What is the difference between a Role and RoleBinding?", back: "A Role defines permissions; a RoleBinding grants those permissions to subjects." },
  { id: "k8s-fc-statefulset", front: "What is a StatefulSet?", back: "A controller for ordered Pods that need stable identities or persistent storage." },
  { id: "k8s-fc-daemonset", front: "What is a DaemonSet?", back: "A controller that ensures a Pod runs on every matching Node." },
  { id: "k8s-fc-hpa", front: "What does HPA do?", back: "It automatically adjusts workload replicas based on metrics such as CPU or memory." },
  { id: "k8s-fc-selector", front: "What is a selector?", back: "A label query used to identify the resources managed or targeted by another resource." },
  { id: "k8s-fc-apply", front: "What does `kubectl apply` do?", back: "It declaratively creates or updates resources from configuration files." },
];

const legacyKubernetesMultipleChoice = [
  ["pod-unit", "What is the smallest deployable Kubernetes unit?", ["Container", "Pod", "Node", "ReplicaSet"], 1, "A Pod is Kubernetes' smallest deployable unit."],
  ["deployment-controller", "Which resource manages rolling updates for stateless Pods?", ["Deployment", "Service", "ConfigMap", "Job"], 0, "Deployments manage ReplicaSets and rolling updates."],
  ["replicaset", "What is the primary job of a ReplicaSet?", ["Route traffic", "Maintain a replica count", "Store secrets", "Schedule CronJobs"], 1, "A ReplicaSet maintains the desired number of matching Pods."],
  ["daemonset", "Which workload normally runs one Pod per Node?", ["StatefulSet", "Job", "DaemonSet", "HPA"], 2, "DaemonSets place Pods on matching Nodes."],
  ["statefulset", "Which controller provides stable Pod identities?", ["Deployment", "StatefulSet", "ReplicaSet", "Ingress"], 1, "StatefulSets preserve ordered, stable identities."],
  ["job", "Which resource runs a task to completion?", ["Job", "Service", "DaemonSet", "Role"], 0, "Jobs manage finite work."],
  ["cronjob", "Which resource schedules recurring Jobs?", ["HPA", "CronJob", "Deployment", "Ingress"], 1, "CronJobs create Jobs from a schedule."],
  ["hpa", "What does an HPA change?", ["Node count", "Service ports", "Workload replicas", "Namespaces"], 2, "HPA scales replicas from metrics."],
  ["clusterip", "Which Service type is cluster-internal by default?", ["NodePort", "ClusterIP", "LoadBalancer", "Ingress"], 1, "ClusterIP exposes a virtual IP inside the cluster."],
  ["nodeport", "Which Service type opens a port on every Node?", ["ClusterIP", "ExternalName", "NodePort", "Headless"], 2, "NodePort exposes a static Node port."],
  ["loadbalancer", "Which Service type requests provider-managed external access?", ["LoadBalancer", "ClusterIP", "Headless", "ConfigMap"], 0, "LoadBalancer integrates with supported infrastructure."],
  ["ingress", "What must exist for Ingress rules to take effect?", ["A DaemonSet only", "An Ingress controller", "A PersistentVolume", "A RoleBinding"], 1, "Ingress resources require a controller."],
  ["service-selector", "How does a Service normally find its Pods?", ["Container image", "Node name", "Label selector", "Namespace UID"], 2, "Services select Pods by labels."],
  ["configmap", "Where should non-sensitive app configuration usually live?", ["Secret", "ConfigMap", "Role", "PVC"], 1, "ConfigMaps hold non-sensitive configuration."],
  ["secret", "Which resource is intended for credentials?", ["ConfigMap", "Secret", "Namespace", "Ingress"], 1, "Secrets hold sensitive values."],
  ["namespace", "Which resource provides a logical namespaced boundary?", ["Node", "Namespace", "ClusterIP", "PV"], 1, "Namespaces scope many resource names and policies."],
  ["pvc", "What does a PersistentVolumeClaim represent?", ["A storage request", "A Node disk", "A container mount only", "A network endpoint"], 0, "A PVC requests persistent storage."],
  ["pv", "Which storage resource generally exists independently of a Pod?", ["emptyDir", "PersistentVolume", "ConfigMap volume", "Projected volume"], 1, "PersistentVolumes have an independent lifecycle."],
  ["serviceaccount", "What identity is assigned to a Pod for API access?", ["Role", "ServiceAccount", "Secret type", "NodePort"], 1, "Pods use ServiceAccounts as workload identities."],
  ["role", "What does a namespaced Role contain?", ["Subjects", "Permissions", "Container images", "Service endpoints"], 1, "Roles define allowed API actions."],
  ["rolebinding", "What does a RoleBinding do?", ["Defines a new API", "Grants a Role to subjects", "Creates a ServiceAccount token only", "Binds storage"], 1, "RoleBindings connect permissions to identities."],
  ["apply", "Which command declaratively creates or updates a manifest?", ["kubectl run", "kubectl apply -f", "kubectl describe", "kubectl exec"], 1, "`apply -f` reconciles manifest configuration."],
  ["describe", "Which command is best for detailed events and conditions on one Pod?", ["kubectl get", "kubectl describe", "kubectl logs", "kubectl top"], 1, "`describe` includes detailed status and events."],
  ["logs", "Which command reads a Pod container's standard output?", ["kubectl logs", "kubectl exec", "kubectl expose", "kubectl explain"], 0, "`logs` reads container output."],
  ["selector", "Which selector matches label `app=api`?", ["--field app=api", "-l app=api", "--name api", "-n app"], 1, "`-l` or `--selector` filters by labels."],
].map(([id, question, options, answer, explanation]) => ({
  id: `k8s-mc-${id}`,
  question,
  options,
  answer,
  explanation,
}));

const legacyKubernetesCommandQuiz = [
  ["pods", "List all Pods in the current namespace.", ["kubectl get pods", "kubectl get po"]],
  ["pods-dev", "List Pods in the `dev` namespace.", ["kubectl get pods -n dev", "kubectl get pods --namespace dev", "kubectl get po -n dev"]],
  ["services", "List Services in the current namespace.", ["kubectl get services", "kubectl get svc"]],
  ["describe-nginx", "Describe the Pod named `nginx`.", ["kubectl describe pod nginx", "kubectl describe po nginx"]],
  ["logs-backend", "View logs for the Pod `backend`.", ["kubectl logs backend"]],
  ["follow-logs", "Follow logs for the Pod `backend`.", ["kubectl logs -f backend", "kubectl logs --follow backend"]],
  ["exec-backend", "Open an interactive `sh` shell in Pod `backend`.", ["kubectl exec -it backend -- sh", "kubectl exec --stdin --tty backend -- sh", "kubectl exec -it backend -- /bin/sh"]],
  ["apply", "Apply the manifest `app.yaml`.", ["kubectl apply -f app.yaml", "kubectl apply --filename app.yaml"]],
  ["delete-pod", "Delete the Pod named `nginx`.", ["kubectl delete pod nginx", "kubectl delete po nginx"]],
  ["deployments", "List Deployments.", ["kubectl get deployments", "kubectl get deployment", "kubectl get deploy"]],
  ["scale", "Scale Deployment `web` to 3 replicas.", ["kubectl scale deployment web --replicas=3", "kubectl scale deploy web --replicas=3", "kubectl scale deployment/web --replicas=3"]],
  ["restart", "Restart Deployment `web`.", ["kubectl rollout restart deployment web", "kubectl rollout restart deployment/web", "kubectl rollout restart deploy web"]],
  ["events", "List cluster events sorted by creation time.", ["kubectl get events --sort-by=.metadata.creationTimestamp", "kubectl get event --sort-by=.metadata.creationTimestamp"]],
  ["all-dev", "Get common workload and Service resources in namespace `dev`.", ["kubectl get all -n dev", "kubectl get all --namespace dev"]],
  ["create-namespace", "Create a Namespace named `dev`.", ["kubectl create namespace dev", "kubectl create ns dev"]],
  ["rollout-status", "Check rollout status for Deployment `web`.", ["kubectl rollout status deployment web", "kubectl rollout status deployment/web", "kubectl rollout status deploy web"]],
  ["undo", "Undo the latest rollout of Deployment `web`.", ["kubectl rollout undo deployment web", "kubectl rollout undo deployment/web", "kubectl rollout undo deploy web"]],
  ["label-pods", "List Pods with label `app=backend`.", ["kubectl get pods -l app=backend", "kubectl get pods --selector app=backend", "kubectl get po -l app=backend"]],
  ["wide", "List Pods using wide output.", ["kubectl get pods -o wide", "kubectl get po -o wide", "kubectl get pods --output wide"]],
  ["service-yaml", "Get Service `backend` as YAML.", ["kubectl get service backend -o yaml", "kubectl get svc backend -o yaml", "kubectl get service backend --output yaml"]],
  ["all-namespaces", "List Pods across all namespaces.", ["kubectl get pods -A", "kubectl get pods --all-namespaces", "kubectl get po -A"]],
  ["configmaps", "List ConfigMaps.", ["kubectl get configmaps", "kubectl get configmap", "kubectl get cm"]],
  ["secrets", "List Secrets in namespace `dev`.", ["kubectl get secrets -n dev", "kubectl get secret -n dev", "kubectl get secrets --namespace dev"]],
  ["top-pods", "Show Pod CPU and memory usage.", ["kubectl top pods", "kubectl top pod", "kubectl top po"]],
  ["contexts", "Show the current kubectl context.", ["kubectl config current-context"]],
].map(([id, prompt, answers]) => ({
  id: `k8s-cmd-${id}`,
  prompt,
  answers,
  explanation: `A valid answer is \`${answers[0]}\`. Common kubectl aliases and long flags are accepted where appropriate.`,
}));

const core = (kind, name) => [
  k.apiVersion("v1"),
  k.kind(kind),
  k.metadataName(name),
];

const apps = (kind, name) => [
  k.apiVersion("apps/v1"),
  k.kind(kind),
  k.metadataName(name),
];

const baseKubernetesManifestPractice = [
  {
    id: "k8s-yaml-pod",
    title: "Nginx Pod",
    filename: "pod.yaml",
    instruction: "Write a Pod manifest named `nginx` using the `nginx` image.",
    starter: "apiVersion: v1\nkind: Pod\nmetadata:\n  name: nginx\nspec:\n",
    solution: "apiVersion: v1\nkind: Pod\nmetadata:\n  name: nginx\nspec:\n  containers:\n    - name: nginx\n      image: nginx\n",
    explanation: "A Pod needs core/v1, Pod kind, metadata, a spec, and at least one container image.",
    rules: [...core("Pod", "nginx"), k.key("metadata"), k.key("spec"), k.key("containers"), k.image("nginx")],
  },
  {
    id: "k8s-yaml-deployment",
    title: "Nginx Deployment",
    filename: "deployment.yaml",
    instruction: "Write a Deployment named `nginx` with 3 replicas and Pods labeled `app: nginx` using the nginx image.",
    starter: "apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: nginx\nspec:\n",
    solution: "apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: nginx\nspec:\n  replicas: 3\n  selector:\n    matchLabels:\n      app: nginx\n  template:\n    metadata:\n      labels:\n        app: nginx\n    spec:\n      containers:\n        - name: nginx\n          image: nginx\n",
    explanation: "A Deployment selector must match labels on its Pod template, and the template defines the containers.",
    rules: [...apps("Deployment", "nginx"), k.key("metadata"), k.key("spec"), k.replicas(3), k.key("selector"), k.key("matchLabels"), k.key("template"), k.key("containers"), k.image("nginx"), k.value("app", "nginx")],
  },
  {
    id: "k8s-yaml-clusterip",
    title: "Backend ClusterIP Service",
    filename: "service.yaml",
    instruction: "Write a ClusterIP Service named `backend-service` selecting `app: backend`, with port 80 targeting 8080.",
    starter: "apiVersion: v1\nkind: Service\nmetadata:\n  name: backend-service\nspec:\n",
    solution: "apiVersion: v1\nkind: Service\nmetadata:\n  name: backend-service\nspec:\n  type: ClusterIP\n  selector:\n    app: backend\n  ports:\n    - port: 80\n      targetPort: 8080\n",
    explanation: "A Service selects Pods and maps its stable port to the application's target port.",
    rules: [...core("Service", "backend-service"), k.value("type", "ClusterIP"), k.key("selector"), k.value("app", "backend"), k.servicePort(80), k.targetPort(8080)],
  },
  {
    id: "k8s-yaml-nodeport",
    title: "NodePort Service",
    filename: "nodeport.yaml",
    instruction: "Write a NodePort Service named `web` selecting `app: web`, port 80, targetPort 8080, and nodePort 30080.",
    starter: "apiVersion: v1\nkind: Service\nmetadata:\n  name: web\nspec:\n",
    solution: "apiVersion: v1\nkind: Service\nmetadata:\n  name: web\nspec:\n  type: NodePort\n  selector:\n    app: web\n  ports:\n    - port: 80\n      targetPort: 8080\n      nodePort: 30080\n",
    explanation: "NodePort extends a Service by opening a valid static port on every Node.",
    rules: [...core("Service", "web"), k.value("type", "NodePort"), k.value("app", "web"), k.servicePort(80), k.targetPort(8080), k.value("nodePort", 30080)],
  },
  {
    id: "k8s-yaml-configmap",
    title: "Application ConfigMap",
    filename: "configmap.yaml",
    instruction: "Write a ConfigMap named `app-config` containing `LOG_LEVEL: info`.",
    starter: "apiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: app-config\n",
    solution: "apiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: app-config\ndata:\n  LOG_LEVEL: info\n",
    explanation: "ConfigMap values normally live under the data mapping.",
    rules: [...core("ConfigMap", "app-config"), k.key("data"), k.value("LOG_LEVEL", "info")],
  },
  {
    id: "k8s-yaml-secret",
    title: "Application Secret",
    filename: "secret.yaml",
    instruction: "Write an Opaque Secret named `app-secret` using stringData with `PASSWORD: changeme`.",
    starter: "apiVersion: v1\nkind: Secret\nmetadata:\n  name: app-secret\n",
    solution: "apiVersion: v1\nkind: Secret\nmetadata:\n  name: app-secret\ntype: Opaque\nstringData:\n  PASSWORD: changeme\n",
    explanation: "stringData accepts clear input that the API server converts into Secret data.",
    rules: [...core("Secret", "app-secret"), k.value("type", "Opaque"), k.key("stringData"), k.value("PASSWORD", "changeme")],
  },
  {
    id: "k8s-yaml-ingress",
    title: "API Ingress",
    filename: "ingress.yaml",
    instruction: "Write an Ingress named `api-ingress` routing path `/api` with Prefix matching to `backend-service` port 80.",
    starter: "apiVersion: networking.k8s.io/v1\nkind: Ingress\nmetadata:\n  name: api-ingress\nspec:\n",
    solution: "apiVersion: networking.k8s.io/v1\nkind: Ingress\nmetadata:\n  name: api-ingress\nspec:\n  rules:\n    - http:\n        paths:\n          - path: /api\n            pathType: Prefix\n            backend:\n              service:\n                name: backend-service\n                port:\n                  number: 80\n",
    explanation: "Ingress v1 routes through backend.service and requires a pathType.",
    rules: [k.apiVersion("networking.k8s.io/v1"), k.kind("Ingress"), k.metadataName("api-ingress"), k.ingressPath("/api"), k.value("pathType", "Prefix"), expectedYamlValue("name", "backend-service", "backend service: backend-service"), k.value("number", 80)],
  },
  {
    id: "k8s-yaml-namespace",
    title: "Development Namespace",
    filename: "namespace.yaml",
    instruction: "Write a Namespace manifest named `dev`.",
    starter: "apiVersion: v1\nkind: Namespace\nmetadata:\n",
    solution: "apiVersion: v1\nkind: Namespace\nmetadata:\n  name: dev\n",
    explanation: "Namespaces are cluster-scoped resources with core/v1.",
    rules: [...core("Namespace", "dev"), k.key("metadata")],
  },
  {
    id: "k8s-yaml-serviceaccount",
    title: "Application ServiceAccount",
    filename: "serviceaccount.yaml",
    instruction: "Write a ServiceAccount named `app-sa` in namespace `dev`.",
    starter: "apiVersion: v1\nkind: ServiceAccount\nmetadata:\n",
    solution: "apiVersion: v1\nkind: ServiceAccount\nmetadata:\n  name: app-sa\n  namespace: dev\n",
    explanation: "ServiceAccounts are namespaced workload identities.",
    rules: [...core("ServiceAccount", "app-sa"), k.value("namespace", "dev")],
  },
  {
    id: "k8s-yaml-role",
    title: "Pod Reader Role",
    filename: "role.yaml",
    instruction: "Write a Role named `pod-reader` that can get, list, and watch Pods.",
    starter: "apiVersion: rbac.authorization.k8s.io/v1\nkind: Role\nmetadata:\n  name: pod-reader\nrules:\n",
    solution: "apiVersion: rbac.authorization.k8s.io/v1\nkind: Role\nmetadata:\n  name: pod-reader\nrules:\n  - apiGroups: [\"\"]\n    resources: [\"pods\"]\n    verbs: [\"get\", \"list\", \"watch\"]\n",
    explanation: "A Role rule combines API groups, resources, and allowed verbs.",
    rules: [k.apiVersion("rbac.authorization.k8s.io/v1"), k.kind("Role"), k.metadataName("pod-reader"), requiredString("Pods resource", "pods"), k.rbacVerb("get"), k.rbacVerb("list"), k.rbacVerb("watch")],
  },
  {
    id: "k8s-yaml-rolebinding",
    title: "Pod Reader RoleBinding",
    filename: "rolebinding.yaml",
    instruction: "Bind Role `pod-reader` to ServiceAccount `app-sa` with a RoleBinding named `read-pods`.",
    starter: "apiVersion: rbac.authorization.k8s.io/v1\nkind: RoleBinding\nmetadata:\n  name: read-pods\n",
    solution: "apiVersion: rbac.authorization.k8s.io/v1\nkind: RoleBinding\nmetadata:\n  name: read-pods\nsubjects:\n  - kind: ServiceAccount\n    name: app-sa\nroleRef:\n  apiGroup: rbac.authorization.k8s.io\n  kind: Role\n  name: pod-reader\n",
    explanation: "RoleBinding subjects identify who receives the permissions, while roleRef identifies the Role.",
    rules: [k.apiVersion("rbac.authorization.k8s.io/v1"), k.kind("RoleBinding"), k.metadataName("read-pods"), k.key("subjects"), requiredString("ServiceAccount subject", "ServiceAccount"), expectedYamlValue("name", "app-sa", "subject name: app-sa"), k.key("roleRef"), requiredString("Role reference", "pod-reader")],
  },
  {
    id: "k8s-yaml-pvc",
    title: "PersistentVolumeClaim",
    filename: "pvc.yaml",
    instruction: "Write a PVC named `app-data` requesting 1Gi of ReadWriteOnce storage.",
    starter: "apiVersion: v1\nkind: PersistentVolumeClaim\nmetadata:\n  name: app-data\nspec:\n",
    solution: "apiVersion: v1\nkind: PersistentVolumeClaim\nmetadata:\n  name: app-data\nspec:\n  accessModes:\n    - ReadWriteOnce\n  resources:\n    requests:\n      storage: 1Gi\n",
    explanation: "A PVC declares access modes and a storage request under resources.requests.",
    rules: [...core("PersistentVolumeClaim", "app-data"), k.key("accessModes"), requiredString("ReadWriteOnce access", "ReadWriteOnce"), k.key("resources"), k.key("requests"), k.value("storage", "1Gi")],
  },
  {
    id: "k8s-yaml-statefulset",
    title: "Postgres StatefulSet",
    filename: "statefulset.yaml",
    instruction: "Write a one-replica StatefulSet named `postgres` using image `postgres:16` and serviceName `postgres`.",
    starter: "apiVersion: apps/v1\nkind: StatefulSet\nmetadata:\n  name: postgres\nspec:\n",
    solution: "apiVersion: apps/v1\nkind: StatefulSet\nmetadata:\n  name: postgres\nspec:\n  serviceName: postgres\n  replicas: 1\n  selector:\n    matchLabels:\n      app: postgres\n  template:\n    metadata:\n      labels:\n        app: postgres\n    spec:\n      containers:\n        - name: postgres\n          image: postgres:16\n",
    explanation: "A StatefulSet needs a serviceName, selector, matching template labels, and Pod template.",
    rules: [...apps("StatefulSet", "postgres"), k.value("serviceName", "postgres"), k.replicas(1), k.key("selector"), k.key("template"), k.image("postgres:16")],
  },
  {
    id: "k8s-yaml-job",
    title: "One-off Job",
    filename: "job.yaml",
    instruction: "Write a Job named `hello` using `busybox` with restartPolicy Never.",
    starter: "apiVersion: batch/v1\nkind: Job\nmetadata:\n  name: hello\nspec:\n",
    solution: "apiVersion: batch/v1\nkind: Job\nmetadata:\n  name: hello\nspec:\n  template:\n    spec:\n      restartPolicy: Never\n      containers:\n        - name: hello\n          image: busybox\n          command: [\"echo\", \"hello\"]\n",
    explanation: "Job Pod templates require a non-Always restart policy.",
    rules: [k.apiVersion("batch/v1"), k.kind("Job"), k.metadataName("hello"), k.key("template"), k.image("busybox"), k.value("restartPolicy", "Never")],
  },
  {
    id: "k8s-yaml-cronjob",
    title: "Scheduled CronJob",
    filename: "cronjob.yaml",
    instruction: "Write a CronJob named `nightly` scheduled for midnight daily, using busybox and restartPolicy OnFailure.",
    starter: "apiVersion: batch/v1\nkind: CronJob\nmetadata:\n  name: nightly\nspec:\n",
    solution: "apiVersion: batch/v1\nkind: CronJob\nmetadata:\n  name: nightly\nspec:\n  schedule: \"0 0 * * *\"\n  jobTemplate:\n    spec:\n      template:\n        spec:\n          restartPolicy: OnFailure\n          containers:\n            - name: nightly\n              image: busybox\n              command: [\"date\"]\n",
    explanation: "A CronJob wraps a Job template and adds a cron schedule.",
    rules: [k.apiVersion("batch/v1"), k.kind("CronJob"), k.metadataName("nightly"), k.value("schedule", "0 0 * * *"), k.key("jobTemplate"), k.image("busybox"), k.value("restartPolicy", "OnFailure")],
  },
];

export const kubernetesManifestPractice = [
  ...baseKubernetesManifestPractice,
  ...kubernetesExtraPractice,
];
