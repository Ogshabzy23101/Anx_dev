services:
  backend:
    image: practice/backend:broken
    ports:
      - "8080:8080"
    environment:
      APP_ENV: production
    depends_on:
      - database

  database:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: app
      POSTGRES_USER: app
      # POSTGRES_PASSWORD is intentionally missing.
